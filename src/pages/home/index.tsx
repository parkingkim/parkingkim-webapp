import Map from '@components/Map';
import MobileView from '@components/MobileView';

const Home = () => {
  return (
    <MobileView>
      <Map />
      home
    </MobileView>
  );
};

export default Home;
